apiVersion: skaffold/v1
kind: Config
build:
  artifacts:
  - image: docker.moppy.tokyo/ip-deroris-net
    docker:
      dockerfile: ./Dockerfile
  local:
    push: true
    useBuildkit: true
deploy:
  kubectl:
    manifests:
    - ./k8s.dev.yaml
